/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: []  // se quiser servir imagens externas, adicione aqui
  }
};
module.exports = nextConfig;
